name: dart_hocr
description: 
  The objective of this project is to come up with a flexibile model for HOCR 
   XML files for CRUD operations in it.
version: 0.1.0
repository: https://github.com/cloudonlanapps/dart_hocr

environment:
  sdk: '>=2.18.2 <3.0.0'

dev_dependencies:
  lints: ^2.0.0
  test: ^1.16.0

dependencies:
  image: ^3.2.2
  xml: ^6.1.0
